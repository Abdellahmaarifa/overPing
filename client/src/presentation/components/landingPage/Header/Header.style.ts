import tw from "twin.macro";

const HeaderContainer = tw.div`
    w-full
    h-screen
    max-h-[900px]
    flex
    flex-col
    justify-between
    items-center
    overflow-x-hidden
    relative
    z-[0]
`;

export default HeaderContainer;
