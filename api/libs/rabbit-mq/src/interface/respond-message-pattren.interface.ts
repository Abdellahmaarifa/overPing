/**
 * Interface for message pattern used to send request to microservice
 *
 * - @property {*} data
 * - @property {string} message
 *
 */
export interface IRespondMessagePattern{
    data: any;
    message: string;
  }
  